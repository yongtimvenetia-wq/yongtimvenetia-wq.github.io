Drop your image files into this folder.

Then reference them from content.js by filename, like this:
  image: "images/my-photo.jpg"

Recommended formats: .jpg or .png
Recommended max width: 1600 pixels (smaller files load faster).
